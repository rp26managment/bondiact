.DS_Store
*.DS_Store
Thumbs.db

# Variables de entorno — NUNCA subir al repo
.env
.env.local
.env.production
.env.development
.env*.local

# Certificados y llaves privadas
*.pem
*.key
*.p12

# Respaldos locales pre-edición (no subir al repo)
*.bak-pre-*

# Datos internos / PII — NUNCA subir al repo público (LFPDPPP)
# Bases de leads, finanzas, trackers y cualquier hoja de cálculo interna
*.xlsx
*.xls
*.csv

# Archivos temporales / lock de Office y LibreOffice
.~lock.*
~$*

# Diseños en sandbox — solo locales, no desplegar a Vercel
sandbox/

# Auditorías de seguridad — solo locales, jamás al repo público (hallazgo carry-over cerrado 2026-08-01)
security-audits/
