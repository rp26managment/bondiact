# AUDITORÍA SEMANAL DE SEGURIDAD — bondíaCT (bondiact.io / ct.bondiact.io)

**Fecha:** Lunes 29 de junio de 2026
**Ejecución:** Automática (scheduled task `bondiact-seguridad-semanal`) — Rod no presente
**Estado:** ⚠️ 1 hallazgo (carry-over de la semana pasada, sin remediar) · 0 incidentes de proveedores NUEVOS · 3 verificaciones manuales pendientes
**Auditoría anterior:** 2026-06-22
**Siguiente auditoría:** Lunes 6 de julio de 2026

---

## Resumen ejecutivo

Semana tranquila en lo nuevo: **sin boletines de proveedores nuevos** que afecten a bondíaCT, whitelist de LIGIE intacta y correcta, y **todos los commits del repo son de Rod** (cero terceros).

El único hallazgo es el mismo de la semana pasada y **sigue abierto**: la carpeta `security-audits/` continúa **trackeada en el repo PÚBLICO** `bondiact`. De hecho creció — el commit `1d77d7d` ("Security", 24-jun) subió la auditoría del 22-jun al repo. Sigue siendo el pendiente #1.

Tres verificaciones sólo las puede cerrar Rod (Firebase, Vercel deployments + toggle "Sensitive", Google Analytics).

---

## 0. Boletines de seguridad de proveedores — ✅ SIN BOLETÍN NUEVO

### Vercel — sin boletín nuevo esta semana
- El único incidente de seguridad sigue siendo el de **abril 2026** (cadena de suministro vía Context.ai → variables NO marcadas "sensitive"). **No es nuevo** — documentado y con plan de acción desde el 20-abr. Su único hilo suelto para bondíaCT: confirmar la rotación de la `VITE_SUPABASE_ANON_KEY` (ver Acciones).
- Lo de finales de junio (19-jun) fue una **incidencia de disponibilidad**, no de seguridad: una regresión de Node.js rompió `node-fetch@2` en algunas Functions; se normalizó solo, sin acción requerida.
- **Relevancia:** ninguna nueva esta semana.

### Supabase — sin advisory crítico nuevo
- **CVE-2026-31813** (bypass de auth) aplica **sólo** a proyectos con providers **Apple/Azure** y se corrigió en 2.185.0. bondíaCT/CT usa auth email/password (Supabase administrado se parchea solo) → **N/A**.
- Pendiente histórico (no urgente): hacia fin de 2026 Supabase aplica control de acceso del Data API a proyectos existentes y retira las API keys legacy (JWT). Planear migración este año.

### Firebase / Google — sin incidente de seguridad
- Los reportes de junio 2026 son de **disponibilidad** (latencia/packet loss en Asia-Pacífico, Cloud Monitoring) — no afectan a México ni son de seguridad.
- Los titulares recurrentes de "apps Firebase exponen datos" son por **Security Rules dejadas en modo público** en otras apps. bondíaCT sólo usa Firebase Auth en `bondiact.io/ligie` con whitelist client-side → sigue vigente el pendiente histórico de migrar a Security Rules.

### GitHub — sin incidente nuevo aplicable
- **CVE-2026-3854** (RCE vía `git push`) se parchó en github.com el 4-mar; el 88% que sigue vulnerable son **GitHub Enterprise Server self-hosted**. bondíaCT usa github.com (SaaS) → **parcheado / N/A**.
- La brecha de **mayo 2026** (repos internos de GitHub vía extensión envenenada de VS Code / Nx Console) afectó repos internos de GitHub, no de clientes; GitHub confirmó que no se explotó en la práctica. **N/A directo**, pero deja una lección concreta para Rod (abajo).

> **Lección barata (no-code):** el vector de mayo (extensión maliciosa de VS Code) y el de abril en Vercel (infostealer Lumma en una laptop) tienen el mismo patrón: comprometen la **máquina del desarrollador**. Tu stack completo cuelga de tu cuenta de GitHub y tu laptop. Vale 2 minutos: confirmar **2FA activo** en GitHub y ser selectivo con extensiones de VS Code.

---

## 1. Accesos LIGIE — Whitelist — ✅ SANO (sin cambios)

Fuente verificada directamente: `ligie/index.html` (array `ALLOWED_EMAILS_B64`, ofuscación Base64 post-Gopak). Decodificado, la whitelist tiene **2 correos, ambos de Rod**:
- `rodrigo@bondiact.io`
- `rodrigopardo6537@gmail.com`

**Sin correos desconocidos. Sin terceros. Sin cambios vs. la semana pasada.**

> 📌 **Nota sobre el checklist:** la tarea programada todavía lista a `antonio@nama.com.mx` como "esperado". Ese correo se **quitó a propósito** en el Sprint SEC-GOPAK (limpieza zero-PII). O sea: la whitelist actual de 2 correos **es la correcta**; el baseline del checklist está desactualizado. Recomendación menor: actualizar la tarea programada para que el "esperado" sean 2 correos, no 3 (así no marca falso positivo cada semana).

> Recordatorio técnico: Base64 **no** es seguridad real (es decodificable, paliativo OSINT). El pendiente de fondo sigue siendo migrar la whitelist a Firebase Security Rules server-side.

---

## 2. GitHub — repo `bondiact` — ✅ SANO

Verificado sobre la copia local (`origin = https://github.com/rp26managment/bondiact.git`, rama `main`).
- **Autoría:** los últimos 15 commits son **100% de Rod** (`Rod Pardo <rodrigopardo6537@gmail.com>`). **Cero autores desconocidos.**
- **Commits recientes legítimos:** `43927c8` (28-jun, quita oferta de acceso de prueba — "nadie tiene acceso en esta fase"), `915f9d0`/`e6cf271` (28-jun, landing MVE-2 + optimización móvil), `5545f90` (26-jun), `1d77d7d` (24-jun, "Security"). Todo consistente con el trabajo en CLAUDE.md.
- **Estado público:** el repo **debe** seguir público (regla crítica: si se hace privado, cae bondiact.io). El remoto apunta a `rp26managment/bondiact`.

*Nota: revisión sobre copia local. Confirmar que el `main` remoto no tenga commits extra requeriría acceso autenticado a la API de GitHub (no verificable desde la auditoría automática).*

---

## 3. Firebase — usuarios y seguridad — ⚠️ NO VERIFICABLE

Requiere acceso autenticado a console.firebase.google.com (proyecto `bondiact-intranet`). No verificable desde la auditoría automática.

**Acción manual Rod:** console.firebase.google.com → `bondiact-intranet` → Authentication → Users → revisar que no haya usuarios desconocidos.

---

## 4. Vercel — deployments — ⚠️ NO VERIFICABLE

Requiere acceso autenticado a vercel.com. No verificable desde la auditoría automática.

**Acción manual Rod:** vercel.com → proyectos `bondiact` y `control-tower` → Deployments → confirmar que los despliegues recientes correspondan a tus commits. Aprovecha para el punto 8 (toggle "Sensitive").

---

## 5. Google Analytics — tráfico — ⚠️ NO VERIFICABLE

Requiere acceso autenticado a analytics.google.com (G-1VYF2B45CJ). No verificable desde la auditoría automática.

**Acción manual Rod (opcional):** revisar picos anómalos de tráfico o referrers raros.

---

## 6. Contraseñas y accesos compartidos — ✅ SIN EVIDENCIA DE CAMBIO

No hay evidencia de accesos nuevos compartidos esta semana: la whitelist de LIGIE no cambió y los commits son todos de Rod. Sin hallazgos.

---

## 7. Certificados y dominios (SSL/HTTPS) — ⚠️ NO VERIFICADO ACTIVAMENTE ESTA SEMANA

Esta semana **no pude** hacer la verificación activa de HTTPS: el sandbox bloqueó tanto `openssl`/`curl` (proxy) como `web_fetch` (restricción de procedencia de URL). Para ser sincero: a diferencia de la semana pasada, **no confirmé con un fetch en vivo** que bondiact.io / ct.bondiact.io respondan por HTTPS hoy.

Mitigaciones que siguen en pie:
- Vercel administra y **renueva TLS automáticamente** (Let's Encrypt); el `vercel.json` aplica **HSTS** y CSP.
- No hubo cambios en `vercel.json` ni en DNS esta semana que pudieran tumbar el certificado.

**Acción manual Rod (10 segundos):** abrir https://bondiact.io y https://ct.bondiact.io y confirmar el candado en el navegador.

---

## 8. Variables de entorno — toggle "Sensitive" — ⚠️ RECORDATORIO (acción manual)

Verificar manualmente en Vercel que estas variables estén marcadas como **"Sensitive"**:
- **ct.bondiact.io** (proyecto control-tower): `VITE_SUPABASE_ANON_KEY`
- **salthub.mx**: `RESEND_API_KEY` (ALTA prioridad — controla envío de email), `RECAPTCHA_SECRET_KEY`

Matiz (sin alarmar): la `anon key` es **pública por diseño** (viaja en el bundle del frontend); su candado real es el **RLS** de Supabase, no ocultarla. La `RESEND_API_KEY` sí es secreto sensible de verdad — esa es la que más importa que esté "Sensitive" y, si quedó expuesta en el incidente Vercel de abril, rotada.

---

## 🟡 HALLAZGO (CARRY-OVER) — `security-audits/` sigue en el repo público — MEDIO

**Qué encontré:** la carpeta `security-audits/` sigue **trackeada en git** dentro del repo **PÚBLICO** `rp26managment/bondiact`, y **no** está en `.gitignore`. Hoy son **5 auditorías** legibles por cualquiera en GitHub (el commit `1d77d7d` del 24-jun agregó la del 22-jun). El hallazgo de la semana pasada **no se ha remediado**.

**Por qué importa:** no es fuga de credenciales rotables, pero es *information disclosure* / reconocimiento gratis: project ref de Supabase, estructura de proyectos de Vercel, nombres de variables, IOC del incidente de abril, un email de cliente en la auditoría 04-20, y el mapa de qué reviso y qué NO cada semana. Justo el tipo de OSINT que buscó el atacante de Gopak en mayo.

**Acción para Rod (no requiere escribir código):**
1. Decidir dónde deben vivir estas auditorías → recomendado: carpeta privada **fuera** del repo público (p. ej. en `Control Tower interior` o el repo privado `Control-Tower`).
2. En GitHub Desktop / Finder: sacar `security-audits/` del repo `bondiact` y agregarla a `.gitignore`. *(Ojo: `.gitignore` no des-trackea lo ya commiteado; hay que `git rm --cached` la carpeta. Borrarla del estado actual la quita de lo público de hoy, pero queda en el historial — si te importa, se purga aparte.)*
3. De paso, confirmar que ningún otro `.md` con datos internos quede trackeado en el repo público.

> ⚠️ Regla #7: yo **no** hago commit/push. Estos cambios los aplicas tú. Esta misma auditoría (29-jun) la guardé **localmente** en `security-audits/` por consistencia, pero **NO está subida** — no se expone hasta que tú hagas commit. Idealmente, muévela junto con las demás a la ubicación privada antes de subir nada.

---

## ACCIONES PARA ROD (por prioridad)

1. **🟡 Sacar `security-audits/` del repo público** `bondiact` (mover a privado + `.gitignore` + `git rm --cached`). **Carry-over — 2da semana abierto.**
2. **Confirmar** si se rotó `VITE_SUPABASE_ANON_KEY` (y, si aplica, `RESEND_API_KEY`) tras el incidente Vercel de abril.
3. **Verificar "Sensitive"** en Vercel: `VITE_SUPABASE_ANON_KEY` (control-tower) y `RESEND_API_KEY` + `RECAPTCHA_SECRET_KEY` (salthub.mx).
4. **Revisar manualmente** Firebase (usuarios), Vercel (deployments), Google Analytics (tráfico).
5. **30 seg de higiene:** confirmar 2FA en GitHub + candado HTTPS en bondiact.io / ct.bondiact.io.
6. *(Menor)* Actualizar el baseline del checklist: la whitelist esperada de LIGIE ahora son **2** correos (sin Antonio).
7. *(Pendiente histórico)* Migrar whitelist LIGIE a Firebase Security Rules; planear antes de oct-2026 el cambio de Data API + migración de API keys legacy de Supabase.

---

## Resumen por chequeo

| # | Chequeo | Estado |
|---|---------|--------|
| 0 | Boletines proveedores (Vercel/Supabase/Firebase/GitHub) | ✅ Sin boletín nuevo aplicable |
| 1 | Whitelist LIGIE | ✅ Sano — 2 correos de Rod, sin cambios |
| 2 | GitHub repo `bondiact` | ✅ 100% commits de Rod · público (correcto) |
| 3 | Firebase usuarios | ⚠️ No verificable — acción manual |
| 4 | Vercel deployments | ⚠️ No verificable — acción manual |
| 5 | Google Analytics | ⚠️ No verificable — acción manual |
| 6 | Accesos compartidos | ✅ Sin evidencia de cambio |
| 7 | SSL / HTTPS | ⚠️ No verificado activamente (restricción del sandbox) |
| 8 | Variables "Sensitive" en Vercel | ⚠️ Recordatorio — acción manual |
| 🟡 | `security-audits/` en repo público | 🟡 Carry-over — sin remediar |

---

*Auditoría generada automáticamente. Tono: directo, sin alarmar. 1 hallazgo accionable (carry-over) + verificaciones manuales pendientes.*
